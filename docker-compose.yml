version: "3.4"
services:
  backend:
    container_name: proj_server
    image: davydq/proj-server
  proxy:
    image: nginx:latest
    volumes:
      - ./nginx.conf:/etc/nginx/nginx.conf
    ports:
      - 5000:80
